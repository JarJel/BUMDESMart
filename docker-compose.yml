services:
  db:
    image: mariadb:11
    container_name: bumdesmart_db
    restart: unless-stopped
    ports:
      - "127.0.0.1:3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: bumdesmart_redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: dzakiawal24/bumdesmart-backend:latest
    container_name: bumdesmart_be
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend/.env:/var/www/html/.env:ro
      - backend_storage:/var/www/html/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost/ -o /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  queue:
    image: dzakiawal24/bumdesmart-backend:latest
    container_name: bumdesmart_queue
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_healthy
    volumes:
      - ./backend/.env:/var/www/html/.env:ro
      - backend_storage:/var/www/html/storage
    entrypoint: []
    command: php artisan queue:work --sleep=3 --tries=3 --timeout=60

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost/api/v1}
        NEXT_PUBLIC_MIDTRANS_CLIENT_KEY: ${MIDTRANS_CLIENT_KEY}
        NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION: ${MIDTRANS_IS_PRODUCTION:-false}
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
        NEXT_PUBLIC_WHATSAPP_GATEWAY_ENABLED: ${NEXT_PUBLIC_WHATSAPP_GATEWAY_ENABLED:-false}
    container_name: bumdesmart_fe
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    container_name: bumdesmart_caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - CADDY_DOMAIN=${CADDY_DOMAIN:-:80}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_started

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: bumdesmart_cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    extra_hosts:
      - "region1.v2.argotunnel.com:198.41.192.57"
      - "region2.v2.argotunnel.com:198.41.200.43"
      - "api.cloudflare.com:104.19.192.29"
      - "host.docker.internal:host-gateway"
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    depends_on:
      - caddy

  openwa:
    image: ghcr.io/rmyndharis/openwa:latest
    container_name: bumdesmart_openwa
    restart: unless-stopped
    volumes:
      - openwa_data:/app/sessions
    ports:
      - "2785:2785"

volumes:
  db_data:
  redis_data:
  backend_storage:
  caddy_data:
  caddy_config:
  openwa_data:
